<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-GLhlTQ8iRABdZLl6O3oVMWSktQOp6b7In1Zl3/Jr59b6EGGoI1aFkw7cmDA6j6gD" crossorigin="anonymous">
  <link rel="stylesheet" href="styles.css">
  <title>Spotify Profiler</title>
</head>

<body>
  <div class="container">
    <% if (user) {%>
      <div class="w-25 m-auto">
        <div class="w-25 m-auto">
          <img class="img-thumbnail rounded-circle w-100 m-auto" src=<%=user?.user.images[0].url %> alt="user">
        </div>
      </div>
      <div class="text-center fw-bold m-3">
        <%= user?user.user.display_name:"" %>
      </div>
      <div class="row text-center w-sm-50">
        <div class="col-md">Followers: <%= user?user.user.followers.total:"" %>
        </div>
        <div class="col-md">Artists Following: <%= user?.followings.artists.total %>
        </div>
        <div class="col-md">Playlists: <%= user?.playlists.total%>
        </div>
      </div>

      <div class="my-5">
        <p class="fw-bold">Playlists</p>
        <% user.playlists.items.forEach((song)=>{%>
          <div class="">
            <%= song.name %>
          </div>
          <%})%>
      </div>


      <div class="my-5">
        <div>
          <p class="fw-bold fs-6">Top Songs</p>
        </div>
        <% user.top_tracks.items.forEach((song)=>{%>
          <div class="">
            <%= song.name %>
          </div>
          <%})%>
      </div>
      <div>
        <div>
          <p class="fw-bold">Top Artists</p>
        </div>
        <% user.top_artists.items.forEach((song)=>{%>
          <div class="">
            <%= song.name %>
          </div>
          <%})%>
      </div>

      <%}else{%>
        <div class="login_screen">
          <div>
            <div class="welcome">
              Welcome to spotify profiler
            </div>
            <div class="sing-in-btn-wrapper">
              <a class="sing-in-btn" href="/authorize">Login with Spotify to continue</a>
            </div>
          </div>

        </div>
        <% } %>

  </div>
</body>

</html>
